sh -c 'git add -A ; git commit -m "source at $(date)" ; git push origin master; harp compile ; rm -r ../ruioliveiras/* ; cp -r www/* ../ruioliveiras/ ; cd ../ruioliveiras; git add -A ; git commit -m "deploy at $(date)" ; git push origin master; '
